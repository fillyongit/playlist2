 <script>
 	// I placeholder __[qualcosa]__ verranno sostituiti nei jsx.
 	const _app = {};
	_app.liveSearchUrl = "{{ path('live_search', {'what': '__what__' }) }}";
	_app.entityUrl = "{{ path('entity_locale', {'what': '__what__', id: '__id__'}) }}";
	_app.saveUrl = "{{ path('entity_save', {'what': '__what__', id: '__id__'}) }}" ;
	_app.userIsAuthenticated = "{{ is_granted('IS_AUTHENTICATED_REMEMBERED') }}";
	_app.userIsAdmin = "{{ is_granted('ROLE_ADMIN') }}";
	_app.userIsEditor = "{{ is_granted('ROLE_EDIT') }}";
	window._app = _app;
 </script>